import React from "react"
import SocialIcon from "./social-icon"
import { PROJECT_NAME } from "../../../constants/Constants"

const Email = () => {
  return <SocialIcon title={`${PROJECT_NAME} - Form`} icon="fa-envelope" />
}

export default Email
